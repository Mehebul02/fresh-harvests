import Spinner from '@/components/shared/loading';
import React from 'react';

const loading = () => {
    return (
        <div>
          <Spinner/>
        </div>
    );
};

export default loading;